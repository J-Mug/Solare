export 'presentation/page_editor_screen.dart';

// NoteEditorScreen is now PageEditorScreen — re-export for router compatibility.
// The router imports NoteEditorScreen; we keep this file as a re-export shim.
// Use PageEditorScreen directly for new code.
